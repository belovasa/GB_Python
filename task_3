# Написать функцию thesaurus(), принимающую в качестве аргументов
# имена сотрудников и возвращающую словарь, в котором ключи — первые буквы
# имён, а значения — списки, содержащие имена, начинающиеся с соответствующей буквы.

def thesaurus(*int_name):
    new_dic = dict()
    for name in int_name:
        new_dic.setdefault(name[0], [])
        new_dic[name[0]].append(name)
    return new_dic


print(thesaurus('Иван', 'Игорь', 'Анна', 'Мария', 'Петр', 'Илья'))